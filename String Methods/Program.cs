
string name = "Luiza";

   name = name.ToUpper();//the toupper is used to put the string in up case, but we have the opposite too

Console.WriteLine(name);
Console.WriteLine(name.Length);//its used by putting a .Length after the variable in the Console.WriteLine

Console.ReadKey();