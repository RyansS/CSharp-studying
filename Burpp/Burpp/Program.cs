Console.Write("How many 'r's'? ");
int answer = int.Parse(Console.ReadLine());
string ars;

Console.ReadKey();
static string burp (int answer, string ars)
{
    if (answer != 0)
    {
        ars = 
    }
    Console.WriteLine("Bu" + "p");
}
